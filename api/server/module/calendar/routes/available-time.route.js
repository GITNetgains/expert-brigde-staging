const controller = require('../controllers/available-time.controller');

module.exports = router => {
  // router.get('/v1/availableTime', Middleware.loadUser, controller.list, Middleware.Response.success('list'));
  // router.post('/v1/availableTime', Middleware.isAuthenticated, controller.create, Middleware.Response.success('create'));
  // router.put('/v1/availableTime/:availableTimeId', Middleware.isAuthenticated, controller.update, Middleware.Response.success('update'));
  // router.delete('/v1/availableTime/:availableTimeId', Middleware.isAuthenticated, controller.remove, Middleware.Response.success('remove'));
};
